import { focusGridDefaultProps } from '../../defaults'
import type { FocusGridProps } from '../../types'

export const focusGridDimUnfocusedProps = {
  ...focusGridDefaultProps,
  dimUnfocused: true,
} satisfies FocusGridProps
